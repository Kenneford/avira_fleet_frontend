node_modules
.env.development
.env.production
.env
.env.example
dist

# Electron build output — generated by the build, never commit (contains
# the packaged app + Avira Fleet.exe which exceeds GitHub's 100MB limit)
release
out
build
*.exe
*.asar
*.pak